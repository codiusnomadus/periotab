require "spec_helper"

RSpec.describe Periotab do
  it "has a version number" do
    expect(Periotab::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
